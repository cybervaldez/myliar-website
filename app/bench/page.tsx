// /bench — prototype of the dynamic-range concept-space browser (the proposed
// homepage). Three versions to pick from; ALL DATA IS FAKE (placeholder prose +
// procedural ASCII keyed to the DR value). Not canon; not the real home yet.
import Bench from "./Bench";

export const metadata = {
  title: "The Bench — concept-space browser (prototype)",
  description: "Dynamic-range browser prototype: scrub · lattice · board. Fake data.",
};

export default function BenchPage() {
  return <Bench />;
}

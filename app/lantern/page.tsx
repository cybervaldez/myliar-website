// /lantern — Phase B de-risk: a lightweight PLAYABLE slice of one fog-world story.
// 6 days across the 5-phase arc; the fog density tracks the arc; the trichotomy
// choice → outcome → stat loop is the real (minimal) gameplay. Authored, not canon.
import Lantern from "./Lantern";

export const metadata = {
  title: "The Lantern — playable slice (Phase B)",
  description: "One fog-world story, 6 days across the arc, playable. The de-risk that the new authoring makes days that play.",
};

export default function LanternPage() {
  return <Lantern />;
}

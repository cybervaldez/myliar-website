// /lantern — Phase B de-risk, §8.15-correct: the player SCRUBS a PERSISTENT surrounding
// condition and DWELLS in it (same weather every day); the 5-phase arc is carried by the
// KEEPER relationship, not the sky. The trichotomy choice → outcome → stat loop is the real
// (minimal) gameplay. Demonstrates THE SETTING IS NOT AN ARC. Authored, not canon.
import Lantern from "./Lantern";

export const metadata = {
  title: "The Lantern — playable slice (§8.15)",
  description: "Pick a persistent surrounding condition and dwell in it; the arc is the Keeper, not the weather. The de-risk that the new authoring makes days that play.",
};

export default function LanternPage() {
  return <Lantern />;
}
